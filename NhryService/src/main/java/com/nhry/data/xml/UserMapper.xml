<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nhry.data.dao.UserMapper" >
	<resultMap id="userMap" type="User">
		<id column="user_id" property="id" jdbcType="BIGINT" />
		<result column="user_name" property="userName" jdbcType="VARCHAR" />
		<result column="email" property="email" jdbcType="VARCHAR" />
	</resultMap>

	<select id="selectByUserName"  parameterType="String"  resultMap="userMap">
		select * from users u where u.user_name like '%' #{userName} '%'
	</select>
	<select id="all" resultMap="userMap">
		select * from users u
	</select>
	<insert id="adduser1"  parameterType="User">
	    insert into users (USER_ID,USER_NAME,COMMENTS) values (#{id},#{userName},#{comments})
	</insert>
</mapper>